[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "querypilot"
version = "0.1.0"
description = "A minimal tool-using SQL agent runtime"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
